package net.afterday.compas.engine.engine.system

import io.reactivex.rxjava3.subjects.BehaviorSubject
import net.afterday.compas.engine.persistency.PersistencyProvider
import net.afterday.compas.engine.sensors.SensorsProvider
import org.mockito.Mockito.mock
import org.testng.annotations.Test

class InfluenceSystemTest {
    /*var playerLevel = BehaviorSubject.create<Integer>()
    var sensorsProvider = mock(SensorsProvider::class.java)
    var persistencyProvider = mock(PersistencyProvider::class.java)
    var gameRunning = BehaviorSubject.create<Long>()
    var influnceSystem = InfluenceSystem(playerLevel, gameRunning)

    @Test fun `empact`(){

    }*/
}